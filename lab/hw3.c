#include <stdio.h>

int main(){
    int n, m, i; 
    scanf("%d", &n);
    float height[n], weight[n];
    for (i = 0; i < n; i++)
        scanf("%f%f", &height[i], &weight[i]);
    float BMI[n];
    for (i = 0; i < n; i++)
    {
        /* code */
    }
    
    int look_for[m];
    for ( i = 0; i < m; i++)
        scanf("%d", &look_for[i]);
    for ( i = 0; i < m; i++)
        Look_for(n, look_for[i], class);
    return 0;
}
